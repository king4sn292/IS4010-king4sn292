# Placeholder lab07.py
# The CI workflow for week07 checks for the existence of this file before
# running the tests.  It doesn't need to contain any logic for this lab, but
# we import the two primary modules here so that running this file exercises
# them at least minimally.

from lab07_contact_book import save_contacts_to_json, load_contacts_from_json
from lab07_api_client import get_api_data


def main():
    print("Lab 07 placeholder module. Modules have been imported successfully.")


if __name__ == '__main__':
    main()
